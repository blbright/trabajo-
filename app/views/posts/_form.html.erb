<%= form_for @post do |f| %>
  <div class="field">
    <%= f.label :post %><br>
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.hidden_field :user_id, value: current_user.id %>
  </div>
  <div class="actions">
    <%= f.submit class: 'btn btn-info'%>
  </div>
<% end %>
