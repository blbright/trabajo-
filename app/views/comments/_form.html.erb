<%= form_for([@post, @comment]) do |f| %>
  <%= f.label :post_id %>
  <%= f.text_field :post_id%>

  <%= f.hidden_field :user_id, value: current_user.id %>

  <%= f.label :thought %>
  <%= f.text_area :thought %>

  <%= f.submit %>
<% end %>
